extends: LibriBrain100.yaml

model:
  context_mode: singleton

training:
  output_dir: outputs/LibriBrain100/word_decoding_3s_single_word_transformer

provenance:
  window_ablation: canonical_3s_signal_window
  context_condition: single_word_transformer
