extends: LibriBrain100.yaml

dataset:
  window_seconds: 1.0
  eligibility_window_seconds: 3.0

cache:
  event_table: tasks/word_decoding/LibriBrain100/cache/LibriBrain100_events_1s_fixed_3s_support.csv

model:
  context_mode: grouped

training:
  output_dir: outputs/LibriBrain100/word_decoding_1s_sentence_transformer

provenance:
  window_ablation: 1s_signal_window_fixed_3s_event_support
  context_condition: sentence_transformer
