extends: ../base.yaml

experiment:
  task: word_decoding
  dataset: libribrain100
  subject_scope: sub0
  id: main_word
  category: main

model:
  use_transformer: false

provenance:
  context_condition: conv_only
