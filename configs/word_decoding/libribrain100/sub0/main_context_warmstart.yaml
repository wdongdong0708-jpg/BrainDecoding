extends: ../base.yaml

experiment:
  task: word_decoding
  dataset: libribrain100
  subject_scope: sub0
  id: main_context_warmstart
  category: main

model:
  use_transformer: true
  context_mode: grouped

training:
  warm_start_from: main_word
  freeze_brain_encoder_epochs: 1

provenance:
  context_condition: sentence_transformer
  initialization: main_word_brain_encoder_only_freeze_epoch_1_then_joint_training
